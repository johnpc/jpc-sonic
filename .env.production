# Production Environment Configuration

# CORS Configuration - Only allow production origins
ALLOWED_ORIGINS=https://sonic.jpc.io

# Static Build Configuration - Use production server
WEBSOCKET_SERVER_URL=https://sonic.jpc.io

# Production mode
NODE_ENV=production

# AWS Configuration for production
AWS_REGION=us-east-1
