# Development Environment Configuration

# CORS Configuration (comma-separated list of allowed origins)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,http://127.0.0.1:3000,http://127.0.0.1:8080,http://localhost:5173,http://localhost:4173,http://localhost:8000

# Static Build Configuration - Use local server for development
WEBSOCKET_SERVER_URL=http://localhost:3000

# Development mode
NODE_ENV=development

# AWS Configuration (if needed for local development)
# AWS_REGION=us-east-1
# AWS_PROFILE=default
